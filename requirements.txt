# Web Framework (uvicorn incluido)
fastapi[standard]==0.128.0

# Data Validation
pydantic==2.7.0

# Data Processing (necesario para que el modelo funcione)
pandas==2.0.3

# Serializaci\u00f3n (compatible con el modelo entrenado)
dill==0.4.1

# Machine Learning (misma versi\u00f3n que el entrenamiento)
scikit-learn==1.7.2

# HTTP Requests
requests==2.31.0